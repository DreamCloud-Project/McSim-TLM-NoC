// should be less than period, don't need to change, if clock frequency is less than 1GHz
#define NEXT_TRIGGER_INTERVAL 1

//
#define ONE_HOP_DELAY 4
#define ONE_ROUTER_DELAY 5

// Number of bytes in a packet (considering a given flit size in bits, this also define the number of flits)
#define PACKET_SIZE_IN_BYTES 32
#define FLIT_SIZE_IN_BYTES 4
